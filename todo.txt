TODO:
x Find best design pattern for config module
- Actually test if the remote repository is eligible. i.e. if it matches the local repository.
- Add "text=True" as argument to all subprocess calls so we don't have to decode stdout/stderr manually.
- Implement setting the editor to the $EDITOR env variable
